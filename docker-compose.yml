version: "3.9"

services:
  bot:
    build: .
    container_name: invoiceBot
    restart: always
    env_file:
      - .env
    working_dir: /app
    volumes:
      - ./handlers/invoice.docx:/app/handlers/invoice.docx:ro